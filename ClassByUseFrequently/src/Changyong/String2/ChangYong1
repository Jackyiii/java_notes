char charAt(int index);获取index位置的字符
boolean contains（CharSequence s）判断字符中是否包含某个字符串
boolean endsWith（String endStr）判断是否是以某个字符串结尾
boolean equalslgnoreCase（String anotherString）忽略大小写比较字符串是否相等
byte[] getBytes();转换成byte数组
int indexOf（String str）取得指定字符在字符串的位置
int indexOf（String str，int fromIndex）从指定的下标开始取得制指定字符在字符串的位置
int lastIndex（String str） 从后面开始取得指定字符在字符串最后出现的位置
int lasIndexOf（String str int fromIndex）从后面开始指定的下标开始取得指定的字符在字符串的位置
int length（）获取字符串的长度
String replaceAll(String s1,String s2)替换字符串中的内容
String[]split(String s)根据指定的表达式拆分字符串
boolean startsWith（String s）判断是否一个某个字符开始
String substring（int begin）更具传入的索引位置截取子串
String substring（int beginIndex，int endIndex）根据传入的起始和结束位置截取子串
char[] toCharArray()将字符串转换为char类型的数组
void toUpperCase转换为大写
void toLowerCase转换为小写
String trim（）去除空格
StringvalueOf（Object obj）将其他类型转换为字符串类型